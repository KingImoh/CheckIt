import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.checkIt.app",
  appName: "CheckIt",
  webDir: "dist",
  bundledWebRuntime: false,
};

export default config;
